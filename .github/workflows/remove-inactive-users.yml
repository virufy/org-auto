name: Track User Activity and Remove Inactive Users

on:
  schedule:
    - cron: '0 0 * * 0'  # Runs at 00:00 every Sunday
  workflow_dispatch:

jobs:
  fetch-user-activity:
    name: Fetch User Activity and Remove Inactive Users
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install jq (for JSON processing)
        run: sudo apt-get install jq -y

      - name: Fetch Organization Members (with Pagination)
        run: |
          # Fetch organization members logic...

      - name: Fetch User Activity for Each Member
        run: |
          # Fetch user activity logic...

      - name: Authenticate GitHub CLI
        run: echo "${{ secrets.ORGANIZATION_ACCESS_TOKEN }}" | gh auth login --with-token

      - name: Remove Inactive Users and Generate Report
        run: |
          # Remove inactive users and generate report logic...

      - name: Upload Updated User List Report
        uses: actions/upload-artifact@v4
        with:
          name: updated-user-list-report
          path: updated_users.json

      - name: Send Email Report (Optional)
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.EMAIL_PASS }}
          subject: Weekly User Activity Report - Virufy GitHub Organization
          body: file://activity_report.txt
          to: ${{ secrets.REPORT_EMAIL }}
          from: GitHub Actions
